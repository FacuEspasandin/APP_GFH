# hoisted y no isolated: Metro (React Native) no resuelve los symlinks de pnpm
# y falla al empaquetar. Es el modo que Expo documenta para monorepos pnpm.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
